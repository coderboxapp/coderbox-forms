import styled from 'styled-components'

export const DatePick = styled.div`
  & .years {
    max-width: 100px;
  }

  & .months {
    max-width: 150px;
  }

  & .menu {
    max-height: 480px;
    overflow-y: auto;
  }
`
