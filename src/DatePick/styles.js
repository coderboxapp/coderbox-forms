import styled from 'styled-components'

export const DatePick = styled.div`
  & .years {
    max-width: 90px;
  }

  & .months {
    max-width: 90px;
  }

  & .menu {
    max-height: 480px;
    overflow-y: auto;
  }
`
